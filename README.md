## Setup

Copy and make changes to `.env.example`

```
cp .env.example .env
```

## Start

```
// Build Docker Image
docker build -t bb/cod-dashboard .

// or run
bin/build

// Run
docker-compose up -d
```
